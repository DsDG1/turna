# Archived spike proto

`turna_anki_spike.proto` was never the runtime codec. Phase 1 wire format is
the versioned JSON envelope in `../` and `bridge/src/contract.rs`.

Do not generate Dart or Rust bindings from this proto.
