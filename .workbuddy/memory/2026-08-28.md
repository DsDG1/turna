# 2026-08-28

## 全项目架构分析（只读探查，未改动任何代码）

对 Turna（Flutter 土耳其语学习 App）做了一次完整架构审计。

**规模实测**：lib/ 手写 Dart 124,217 行（生成代码另 24,173 行），586 个 dart 文件，
157 个 view widget，70 个 injectable 类，59 条 AutoRoute 路由，263 个测试文件。
DB schemaVersion 21，约 32 张表（15 个 drift Table 类 + 约 17 张原生 SQL 管理）。

**分层代码量**：application 55.3k / views 46.8k / data 7.1k / domain 5.4k /
service 3.7k / core 2.0k / courses 1.0k / di 0.5k / routing 0.2k。
其中 `application/anki_official/` 独占 26,884 行（104 文件），是全项目最大子系统，占 21.6%。

**架构定性**：Clean Architecture 四层 + core/service/courses/routing/di 横向支撑。
Provider + ChangeNotifier 做状态，GetIt + Injectable 做 DI，AutoRoute 做路由，
drift + SQLite 本地持久化，Freezed 不可变模型。本地优先、无后端。

**发现的架构问题（重要，后续可复用）**：
1. views 层 10 个文件直接 import `data/`（home_page.dart 一处引 3 个）。
2. domain 层 3 个文件反向依赖 application/ 与 data/ —— 真正的依赖倒置：
   `domain/repositories/i_course_repository.dart` 接口引了 `data/course_database.dart` 实现。
3. core 层被污染：`core/text_styles.dart` → `views/theme.dart`；
   `core/fsrs_optimizer.dart` → `data/review_history_dao.dart`。
4. AI 层：`AiEngine` 是具体类无抽象接口，不可替换；API key 经 UI 层传递；
   无统一重试/退避；`AiErrorMapper` 靠字符串嗅探 HTTP 状态码。
5. 文档漂移：project-guide.md 仍写 schemaVersion 18（实际 21）、13 种 Interaction（实际 14）、
   ai/ 下有 hint/wish/course/tutor 目录（实际是 engine/companion/textbook）。

**已验证成立的优点**：AI 引擎确为全应用唯一 LLM 出口（全局仅 2 处 import http，
另一处是 WebDAV 备份）；渲染器插件化（Set<InteractionRenderer> via GetIt，
新增题型不改分发逻辑）；Anki 迁移用 write fence + owner authority 治理得克制。

**平台约束**：Android 仅提供 arm64-v8a 的 libturna_anki.so，iOS/Windows 硬编码不可用。

## 文档漂移修复（4 份文档，未动代码）

修复 `docs/project-guide.md`（89 行）、`CLAUDE.md`、`README.md`、`tool/gui/README.md`。

**修正的事实**：schemaVersion 18→21；13→14 种 Interaction；
`game_milestone_provider.dart`→`gems_provider.dart`；测试基线 1299→1852 passed；
ai/ 子目录 hint/wish/course/tutor→engine/companion/textbook；GUI 编辑器 12→14 种题型。

**顺带查实的重大漂移：Legacy Anki 复刻层已物理删除**（2026-08-27，doc 35 L0–L3 施工完毕）。
`lib/application/anki/` 目录已清空（guard test 有终态断言）。原 §6 整节描述的是已删流程，已重写：
- 已删：`anki_importer` / `anki_deck_assembler` / `anki_srs_migrator` / `anki_type_answer` /
  `AnkiOrganizationResolver` / `AnkiNotetypeAI` / `AnkiRenderPolicy` / Full-Lite 装配。
  导入执行计划只剩 officialFirst / failClosed / unsupported 三态。
- **仍活跃**：官方保真渲染链 `official_formal_review_coordinator` →
  `study_session/anki_review_content.dart` → `official_template_webview_body` →
  `AnkiHtmlCardView`（唯一 WebView 使用点）。
- `ankiHtmlCard` 类型保留但语义分裂：Official 复习用它渲染 HTML；lesson 体/错题重放中的
  存量卡走 `anki_html_card_retired_renderer` 降级为「确认即过」占位卡（防 lookupRenderer 抛 StateError）。

**发现的功能性缺陷（未修，已写入文档 §6.4）**：「智能去解密」pre-render 缓存链路已断——
`onCaptured` 回调全 lib 零传入方，导致 `_scheduleCapture` 恒 return、缓存永不写入；
`AnkiNoteDao.prerendered()` 零调用点。表 `anki_prerendered_html` 只剩存量数据与统计清理。

**判读经验**：这个项目的 docs 与代码会漂移，且删除类变更只记录在
`docs/official-anki-migration/35-*.md` 这类施工文档里，主文档不会同步。
核实「某功能是否还在」时，先 grep 类名的**引用数**（0 引用 = 已删），
再看 `docs/official-anki-migration/` 下的施工实录 §10「与计划的差异」。

---

## 18:20 全项目垃圾代码审计（只读，未改任何生产代码）

产出：`.workbuddy/reports/code-junk-audit-2026-08-28.md`（A/B/C/D 四档处置清单）。

方法（可复用）：`flutter analyze` 打底 + 3 路并行语义扫描（死代码 / 调试残留 / 资源与依赖）
+ 关键条目独立 grep 双重复核。lint 干净 ≠ 没垃圾，本项目 lint 只有 4 条 info，
但语义层挖出 9 个孤儿文件（1,252 行）、1 条四环僵尸链、110 处 debugPrint 盲区。

**扫描时的几个坑**（下次直接复用）：
- extension 名在调用点不出现 → 「名字只出现 1 次」不是死的充分证据，必须下钻到成员名。
- 大小写不敏感搜 `todo` 会命中 `toDouble()`（40 处误报）；搜 `/home/` 命中 `views/home/`；
  搜 `temp` 命中 `template`。
- 条件 import 第二分支（`import x if (dart.library.io) y`）会让 y 被误判孤儿。
- `CanonicalCard` 是 `CanonicalCardKey` 的前缀，grep 必须加词边界。
- `manifest.json` 在 lib/ 的 41 处命中全是同名 `legacy-manifest.json`。
- 找孤儿 dart 文件要解析 import 图（含 `package:turna/` 映射 + 相对路径 normpath），
  包名是 **turna** 不是 varnamalaplus。

**用户未授权删除，本轮零改动。** 报告里 A 档（可立即删）9 个文件 + 5 个 PNG 等着我
下次确认后动手。
