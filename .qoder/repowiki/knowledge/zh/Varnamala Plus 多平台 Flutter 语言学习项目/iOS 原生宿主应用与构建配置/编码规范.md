- AppDelegate 仅保留最简实现，所有插件注册委托给 `GeneratedPluginRegistrant`，避免手写桥接代码。
- 构建配置通过 xcconfig 分层 include（Pods 生成的 debug/release 配置 + Generated.xcconfig），而非直接修改 PBXproj。
- Podfile 使用 Ruby 方法封装 Flutter 集成逻辑（`flutter_root`、`flutter_ios_podfile_setup`、`flutter_install_all_ios_pods`），保持跨项目一致性。