release 签名需将 `key.properties`（含 storeFile/storePassword/keyAlias/keyPassword）放入 `android/` 目录（已在 .gitignore 中排除）；单 ABI 打包通过 `flutter build apk --release --split-per-abi --target-platform android-arm64` 完成，此时不应再设置 `ndk.abiFilters` 以避免冲突。