Flutter Embedding v2 + Kotlin 2.2.20 + AGP 8.11.1 + Gradle Wrapper；启用 Java 11 兼容性（coreLibraryDesugaring）以支持 `flutter_local_notifications` 的 `java.time` API；R8 代码压缩配合 `proguard-rules.pro` 保留 sherpa_onnx JNI、drift、freezed/json 等反射入口。