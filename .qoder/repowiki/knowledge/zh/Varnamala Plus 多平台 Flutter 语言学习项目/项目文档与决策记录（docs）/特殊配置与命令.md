课程内容的校验与导出通过 Python CLI 完成：`python3 tool/course_cli.py validate --course-dir assets/courses/<lang>`（人类可读）与 `--format json`（机器可读），配合 `lint` 与 `export_content_inventory.py` 生成内容清单；Flutter 侧通过 `flutter test` 运行 Dart 验证器以保持规则一致。