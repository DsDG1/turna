- ADR 文档统一采用「Date / Status / Context / Decision / Consequences」五段式结构，并按递增编号（0021、0022…）命名文件。
- 课程 JSON 模板（authoring/templates/*.json）使用 `subLessons → stages → items` 的嵌套结构，每个 item 通过 `runtimeType` 字段标识具体 Interaction 类型。
- 课程版本变更通过 `index.json` 的 `version` 字段（及 `expressions.json` 的 version）组合成 composite reseed key，驱动应用重新播种数据。
- 内容规模约束（≤60 units/section、≤40 lessons/unit）在 Dart 验证器与 Python CLI 两端同时声明，确保作者与工程端契约一致。
- Git 相关配置（clone root、git bin、timeout、LAN 端口、token、SSH key）统一通过 QSettings 持久化，并以 `git/*` 键前缀组织。