Flutter + Provider (状态管理) + auto_route (声明式路由) + get_it + injectable (依赖注入) + drift (SQLite ORM, NativeDatabase) + streaming_shared_preferences (响应式本地存储) + flutter_tts (语音合成) + google_fonts (字体) + logger (结构化日志)。