运行 Flutter 测试：`flutter test`（当前 451 个用例全部通过）；Python CLI 测试：`python3 -m unittest discover -s test -p "*_cli_test.py"`；GUI 工具套件：`python3 -m unittest discover -s tool/gui/tests -p "test_*.py"`（约 804 用例，offscreen 可跑）。Linux 主机需确保 sqlite3 库可用，`in_memory_course_db.dart` 会自动回退到 `libsqlite3.so.0`。Golden 测试依赖固定 surface 尺寸（400×700）和主题切换（light/dark）。