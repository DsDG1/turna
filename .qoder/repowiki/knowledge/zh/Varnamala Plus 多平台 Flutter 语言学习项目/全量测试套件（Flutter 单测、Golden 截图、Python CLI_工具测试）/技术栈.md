Flutter + `flutter_test`（含 `TestWidgetsFlutterBinding.ensureInitialized()` 初始化）、`streaming_shared_preferences` 的 mock 模式、Drift/SQLite（通过 `NativeDatabase.memory()` 内存库）、Python `unittest` + `unittest.mock` 用于 CLI/构建脚本测试。Golden 测试使用 `matchesGoldenFile` 对比 PNG 截图。