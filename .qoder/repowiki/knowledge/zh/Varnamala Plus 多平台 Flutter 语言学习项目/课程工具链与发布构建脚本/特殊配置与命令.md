发布构建：`python tool/build_release.py --version <ver> [--skip-web] [--skip-content-validation] [--output-dir ./dist]`；内容校验：`python tool/course_cli.py validate [--format json]`；TTS 音频生成需设置 `MINIMAX_API_KEY`（可选 `MINIMAX_GROUP_ID`），然后 `python tool/generate_audio.py all/list/speak ...`；A1 混音需先 `pip install pydub` 并确保 ffmpeg 在 PATH 中，再运行 `python tool/mix_listening_a1.py all --mapping tool/mappings/a1_show_mapping.csv`。