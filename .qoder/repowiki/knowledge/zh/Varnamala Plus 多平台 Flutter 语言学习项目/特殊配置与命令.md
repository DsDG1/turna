根级 `make` 命令统一入口：`make gen` 触发 build_runner 生成代码，`make test` 运行 Dart 单测，`make test-python` 运行 Python 工具测试，`make test-gui-*` 分层执行 GUI 测试（L0/L1/L2），`make ci` 串联 analyze+test+test-python+build-release-smoke，`make build-release VERSION=x.y.z` 一键打包全平台产物。