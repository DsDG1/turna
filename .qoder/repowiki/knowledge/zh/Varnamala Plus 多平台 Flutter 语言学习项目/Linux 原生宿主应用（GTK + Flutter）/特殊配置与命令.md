构建流程由 Flutter 工具链驱动：顶层 `CMakeLists.txt` 通过 `add_subdirectory(flutter)` 引入 Flutter 构建规则，并在 `flutter_assemble` 目标上执行 `${FLUTTER_ROOT}/packages/flutter_tools/bin/tool_backend.sh` 生成引擎库与头文件；最终通过 `cmake --build . && cmake --install .` 输出到 `bundle/` 目录。