- 领域模型统一使用 freezed 不可变类 + @JsonSerializable 注解，并通过 build_runner 生成序列化代码
- 依赖注入采用 get_it + injectable，服务按模块注册到 injector_module.dart
- 路由使用 auto_route，页面通过 @RoutePage 注解并集中在 routes.g.dart 生成
- 数据层通过 drift 定义 schema，由 drift_dev 生成 DAO，Repository 接口与实现分离
- 测试按层次组织：domain/application/data/views/golden 对应 lib 目录结构，Golden 截图用于视觉回归