## 修复：正课完成后复习卡显示 "——"

### 根因
复习批次中 `AnkiHtmlCard`（fidelity 卡）被包成 `FidelityCardPresentation`（不带内容），`AnkiReviewSessionPage._contentFor` 无此分支 → 返回空 `StandardCourseCardContent` → 正面显示 "—"。现成的 `OfficialTemplateContent` → `OfficialTemplateWebViewBody` WebView 渲染分支因从未被构造而不可达。算法/调度链路（FSRS ledger、undo、配额）验证完好，无需改动。

### 改动

1. **新建 `lib/application/anki/anki_review_content.dart`**：纯函数 `reviewContentFor(StudyItem item, {Map<String, AnkiHtmlCard>? fidelityInteractions})`，返回 `ReviewContentBodyData`：
   - `FlipCardPresentation` → `StandardCourseCardContent(frontText/backText/backNote)`
   - `StructuredCardPresentation` → `StandardCourseCardContent(prompt/answer/interaction)`（沿用现逻辑）
   - `FidelityCardPresentation` + 对应 `AnkiHtmlCard` → `OfficialTemplateContent(frontHtml, backHtml, mediaBasePath)`
   - 其余兜底空 content（保持现状，不崩）

2. **`lib/views/anki/anki_review_session_page.dart`**：
   - `_start` 组装 items 的循环里，若 `interaction is AnkiHtmlCard`，记入 `Map<sessionItemId, AnkiHtmlCard>`
   - 把该 map 传入 `_AnkiStudySessionView`，`_contentFor` 改为调用新 helper
   - fidelity 卡 `structured == false`，自然走“显示答案”→ BinaryRecallBar 流程，与正课一致

3. **`StudyCardSurface` / `OfficialTemplateWebViewBody` 不改**（渲染分支已存在且接好）。

### 测试
- 新建 `test/application/anki/anki_review_content_test.dart`（纯 Dart 单测）：fidelity+AnkiHtmlCard → `OfficialTemplateContent` 且 html/mediaBasePath 正确；flip/structured 映射不变；fidelity 缺 interaction → 空兜底不抛异常
- 跑 `flutter analyze` 与相关测试（新测试 + `test/views/review/study_card_surface_test.dart` + `test/application/anki/` 现有相关测试）

### 不在范围
- `unified_anki_import_orchestrator.dart` 写死 `payloadJson: '{}'` 的链路：无生产消费者（死脚手架），不动
- `lesson_viewmodel.dart:747` 非白板 `AnkiCard` 的空 front：仅影响 learn 提交的 presentation receipt（不渲染），不动