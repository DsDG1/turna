// Loading entry point for the Swahili course. Course data is bundled as
// JSON under `assets/courses/` and loaded asynchronously by
// `SwahiliCourse.load()`.
//
// NOTE: The on-disk JSON files still contain Kannada data; only the public
// identifiers have been switched to Swahili while the actual content is
// being prepared.
export 'grammar_points.dart';
export 'kannada.dart';
export 'kannada_vocab.dart';
